%++%
`%++%` <- function (a) {a <- a+1}
a
a<-4
a%++%
;
a %++%
;
a %++% 5
`%++%` <- function (a,b) {a <- a+1}
a %++% 5
print(a %++% 5)
print(a %++% 7)
assign
`%++%` <- function (a,b) {assign(as.character(a), a+1}
`%++%` <- function (a,b) {assign(as.character(a)), a+1}
`%++%` <- function (a,b) {assign(as.character(a), a+1)}
a
a%++%8
a
print(a %++% 7)
`%++%` <- function (a,b) {assign(a, a+1)}
print(a %++% 7)
rm `%++%`
rm (`%++%`)
print(a %++% 7)
